# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1536m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Play Console flagged "optimized resource shrinking isn't enabled". This
# flag turns it on explicitly. Automatic on AGP 9.0+ (which this project is
# now on) per Google's docs, but harmless to leave set. Requires
# isMinifyEnabled/isShrinkResources true in the release buildType, which
# app/build.gradle already has.
android.r8.optimizedResourceShrinking=true

# Temporary compatibility opt-outs for AGP 9.0's new android DSL, which
# removed applicationVariants and similar old-style variant APIs that
# third-party plugins (Firebase, RevenueCat, social-login SDKs — this
# project uses all three) commonly still depend on. Added proactively when
# jumping straight to AGP 9.0 without the ability to test-build and iterate
# on whatever broke. Both are Google's own documented escape hatches for
# exactly this situation, and both are slated for REMOVAL in AGP 10.0 — once
# every plugin here is confirmed compatible with the new DSL/built-in
# Kotlin, remove these two lines rather than carrying them forward.
android.newDsl=false
android.builtInKotlin=false
